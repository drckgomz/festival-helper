// src/app/(admin)/layout.tsx
import type { ReactNode } from "react";

export default function AdminGroupLayout({ children }: { children: ReactNode }) {
  // Group layout — keep minimal; real layout is under /admin/layout.tsx
  return children;
}
